/**
 * Defines the typography settings for the application.
 * Using CSS variables like `var(--font-inter)` is a modern practice,
 * especially when using font providers like next/font.
 */
export const typography = {
  fontFamily: 'var(--font-inter)',
  // You can define standard weights to reference in components if needed
  fontWeightRegular: 400,
  fontWeightMedium: 500,
  fontWeightBold: 700,
  fontWeightExtraBold: 800,
};